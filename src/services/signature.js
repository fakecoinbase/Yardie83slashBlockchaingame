export const sign = ({ para1, para2 }) => {
    //FIXME Make me sign correctly
    const sign = para1 + para2;

    return sign;
}

export const check = ({ para1, para2, para3 }) => {

    // FIXME Make me check the signature

    return true;
}
